{
  "name": "webpack-test",
  "version": "1.0.0",
  "description": "just webpack test",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "npm-install": "npm i",
    "webpack": "webpack --progress --colors --watch",
    "webpack-dev-server": "webpack-dev-server --content-base ./src/ --progress -d --colors --inline --hot",
    "webdriver-update": "webdriver-manager update",
    "webdriver-start": "webdriver-manager start",
    "webdriver-stop": "webdriver-manager stop",
    "protractor": "protractor conf.js",
    "e2e-run": "npm run webdriver-start && npm run protractor"
  },
  "author": "Andrey Fomin",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.13.2",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.13.2",
    "css-loader": "^0.23.1",
    "protractor": "^4.0.3",
    "style-loader": "^0.13.1",
    "webdriver-manager": "^10.2.2",
    "webpack": "^1.13.1",
    "webpack-dev-server": "^1.14.1"
  },
  "dependencies": {
    "jquery": "^3.1.0"
  }
}
